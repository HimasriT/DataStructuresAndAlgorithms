1) Used Visual Studio editor.
2) Build the code file.
3) Run the program.-> a) Already the values are hardcoded in the unsorted array. 
		   -> b) Prints the unsorted array.
		   -> c) Heapifies the array and print the Min heap array.
		   -> d) Programs calls heapsort and sorts the array in the descending order.
		   -> e) Prints the sorted array.
Screenshots are attached in Assignment_3 file.
