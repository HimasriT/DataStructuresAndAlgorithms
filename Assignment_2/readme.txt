1) Used Visual Studio editor.
2) Build the code file.
3) Run the program.-> a)  Already the values are hardcoded in the array for tree creation
		   -> b ) After creating the tree, it prints the inorder and levelorder of the tree.
		   -> c) Program asks for the value to be deleted. Enter the value.
		   -> d) After deleting the value, it again prints the levelorder and inorder.
		   -> e) Program asks if there are any more nodes to be deleted. Enter 1 of yes, 0 otherwise.
		   -> f) If 1 is entered, the program repeats from step c).
		   -> g) If 0 is entered, the program exits.

Screenshots are attached in Assignment_2 file.
