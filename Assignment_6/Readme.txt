1) Used Visual Studio editor.
2) Build the code file.
3) Run the program.-> a) The maximum size of the hashtable is 100.
                      b) The input is present in the text file "input.txt"
                      c) Program takes each word , hashes it and inserts into the hashtable(tableSize = 31).
		      d) When the load_factor reaches 0.5, rehashing happens and the tableSize increases to 67 and
			 the old words are again reinserted.
                      e) At the end, it prints the number of collisions and the final table entries.

Screenshots are attached in Assignment_6 file.